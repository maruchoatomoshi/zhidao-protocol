# 🧊 FEATURE FREEZE — план перед запуском

**Создано:** 2026-06-08
**Цель:** довести приложение до стабильного состояния к выдаче доступа, без сюрпризов в Пекине.

## Ключевые даты

| Дата | Что происходит |
|---|---|
| **28 июня** | Выдача доступа реальным студентам (первый настоящий трафик) |
| **4 июля** | Прибытие в Пекин (перекличка и баллы работают «вживую») |

> ⚠️ Главный стресс-тест — **28 июня**, а не 4 июля. Замораживаемся с прицелом на 28-е.

## График заморозки

| Период | Этап | Можно | Нельзя |
|---|---|---|---|
| 8–22 июня | 🟢 Окно доработок | любые фичи, полировка | — |
| **23 июня** | 🟠 **Заморозка фич** | только `fix:` | новые фичи / роуты / таблицы |
| 23–27 июня | 🛡 Закалка | хардненинг, смоук на iPhone, бэкапы | фичи |
| 28 июня | 🚀 Выдача доступа | только хотфиксы по логам | плановые изменения |
| 28 июня – 3 июля | 👀 Мониторинг | критичные баг-фиксы | плановые изменения |
| 4 июля | 🔒 Пекин — локдаун | только аварийные хотфиксы | всё |

## Правила заморозки (с 23 июня)

- В `main` только коммиты с префиксом `fix:`.
- Никаких новых эндпоинтов в `zhidao_api_ready.py`.
- Никаких новых таблиц/миграций БД (риск рассинхрона фронта и старого бэка).
- Перед 28-м снять «золотые» бэкапы на сервере:
  ```bash
  cp /root/zhidao_api.py /root/zhidao_api.backup.py
  cp /root/zhidao_bot.py /root/zhidao_bot.backup.py
  ```

---

# 🔒 Заморозка разделов внутри приложения

Чтобы приложение не «выдало всё сразу» и сохранило интерес, часть разделов закрыта
для обычных игроков до старта. **Админы и архитекторы видят и открывают всё** —
заморозка на них не действует, так что тестировать можно как обычно.

## Замороженные разделы

| Ключ | Раздел | Где открывается |
|---|---|---|
| `casino` | Кейсы / молитвы | нижняя навигация |
| `shop` | Магазин | нижняя навигация |
| `implants` | Импланты / карточки | нижняя навигация |
| `laundry` | Стирка / вода | Ещё → Стирка |
| `achievements` | Ачивки | Ещё → Ачивки |
| `diary-stars` | Дневник ★ | Ещё → Дневник ★ |
| `rating` | Рейтинг | нижняя навигация |

Остаются доступными: главная, расписание, погода, карта кампуса, темы, команда.

## Как это работает

- Заблокированные пункты помечаются замочком 🔒 и приглушаются.
- При нажатии вместо раздела показывается оверлей «Раздел заморожен» с (опциональным)
  таймером до открытия.
- Backend не трогается: вся экономика и данные на месте, закрыт только вход на фронте.

## Где настраивается

Всё в **`js/config.js`**:

```js
window.APP_FEATURE_FREEZE_ENABLED = true;     // главный выключатель всей системы
window.APP_FROZEN_FEATURES = {
  casino:        true,  // кейсы / молитвы
  shop:          true,  // магазин
  implants:      true,  // импланты / карточки
  laundry:       true,  // стирка / вода
  achievements:  true,  // ачивки
  'diary-stars': true,  // дневник ★
  rating:        true,  // рейтинг
};
window.APP_FREEZE_TARGET_AT = '2026-06-28T00:00:00+08:00'; // таймер обратного отсчёта
```

### Как разморозить раздел
Поставить ему `false` и запушить — изменение подтянется через GitHub Pages.

### Как открыть всё разом (на старте)
`window.APP_FEATURE_FREEZE_ENABLED = false;` — мгновенно снимает все замки.

### Таймер
Сейчас таймер скрыт, пока окно заморозки не показывает осмысленный отсчёт.
Ближе к 28 июня можно подсветить его в оверлее — дата уже задана в `APP_FREEZE_TARGET_AT`.

## План разморозки

Открывать можно поэтапно (создаёт ощущение «события»), либо всё разом 28 июня.
Решим ближе к дате. По умолчанию — единое открытие на старте.
