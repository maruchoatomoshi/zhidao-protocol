Поле кода — используй везде, где код выдаёт человек: привязка MAX, комната игры, обмен дублями.

```jsx
<CodeField label="Код привязки" value={code} onChange={setCode} hint="Код выдаёт вожатый" error={err} />
```

Ошибку пиши словами сервера, без догадок о причине.
