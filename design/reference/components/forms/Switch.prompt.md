Переключатель — используй для настроек в профиле: оформление, ночь, «Анимации».

```jsx
<Switch checked={motion} onChange={setMotion} label="Анимации" labelCn="动画" hint="У всего есть вариант без движения" />
```

Флажок «Анимации» обязателен рядом с системным «уменьшить движение».
