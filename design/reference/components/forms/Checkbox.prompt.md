Флажок — используй для необязательной отметки внутри формы или окна свойств.

```jsx
<Checkbox checked={sound} onChange={setSound} label="Звуки" labelCn="声音" />
```

Неготовую настройку показывай с `disabled`, а не убирай.
