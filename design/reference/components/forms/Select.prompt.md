Выпадающий список — используй, когда вариантов больше трёх и они равноправны.

```jsx
<Select label="Оформление" value={skin} onChange={setSkin}
  options={[{value:"aqua",label:"Аква"},{value:"xp",label:"Луна-Аква"}]} />
```

Для двух вариантов возьми вкладки или переключатель.
