Строка состояния — используй внизу окна, чтобы сказать, откуда данные и в каком режиме экран.

```jsx
<StatusBar items={["Сервер: сезон не начался", "Обновлено 18:40", "Офлайн"]} />
```

Это место для оговорок о данных, а не для действий.
