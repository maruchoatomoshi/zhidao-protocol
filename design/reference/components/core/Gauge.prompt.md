Прибор — используй для прогресса пазла и счётных попыток сканера.

```jsx
<Gauge value={3} segments={5} label="Попытки" />
<Gauge value={40} label="Пазл встреч" />
```

На нуле прибор показывает насечки: видно, что данных нет, и он не мигает.
