Вкладки — используй, когда один экран показывает два-три равноправных списка.

```jsx
<Tabs value={tab} onChange={setTab} items={[{id:"shelf",label:"Витрина"},{id:"mine",label:"Моё"}]} panel={content} />
```

Две-четыре вкладки. Больше — это уже раздел в «Ещё», а не вкладка.
