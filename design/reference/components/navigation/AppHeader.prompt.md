Шапка — ставь первой на каждом экране приложения; в Луне-Акве она становится заголовком окна.

```jsx
<AppHeader stars={212} onProfile={() => go("profile")} />
```

Баланс ★ показывается только здесь. Учитывай, что сверху стоит панель MAX: своей строки состояния телефона не рисуем.
