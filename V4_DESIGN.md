# ZHIDAO V4 — «Обещанное завтра»

## XP Home · эскиз 01 · 2026-09-09

По новому референсу пользователя подготовлен отдельный вариант «Сегодня»:
`/app/?design=xp`. Обычный `/app/` сохраняет Aero. Ветка `codex/v4-xp-home`,
база до эскиза `f650eec`. Запрос не записывает настройку в localStorage и
не меняет авторизацию. Ссылка «Сравнить с Aero» возвращает старое оформление.

Изменены главная и общая оконная рамка (шапка, адрес, нижняя навигация).
Содержание остальных экранов ещё не переводилось в XP. Оформление использует
существующие иллюстрацию Хайнаня, логотип пользователя и иконки; новые
картинки, библиотеки и сетевые зависимости не добавлены. Неактивное расписание,
REP и гостевые попытки не заменены выдуманными данными. Настоящие попытки
остаются привязаны к существующему `data-case-scans`.

Стилевой слой: `xp-home.css`; включение по query: `xp-home.js` в head.
Новые элементы `xp-only` скрыты вне эскиза. Дневные панели — непрозрачные,
светлые; ночные — синие с тем же порядком блоков. Системные reduced-motion
и выключение анимации в «Ещё» сохранены. Имитированных крестиков, сворачивания,
прогноза погоды, уведомлений и Windows-логотипов нет.

Проверка: `tools/check_v4_xp_home.cjs`, локальный disposable preview на 8787.
14 сочетаний (2 темы × 7 ширин), загруженные логотипы без пересечения,
основные touch-кнопки не меньше 44 px, четыре быстрых перехода и профиль,
возврат к Aero, ноль работающих анимаций при reduced/off. Скриншоты и JSON:
`.codex-tmp/xp-qa/`. Кейсы: 6 браузерных проверок прошли на временной БД;
asset versions: 3 passed. Настоящий MAX не проверен, сервер не обновлён.
Художественное одобрение — перед переносом остальных экранов и публикацией.

## Идея

ZHIDAO Hainan показывает будущее, которое всё-таки наступило: технологии
дружелюбны, путешествие расширяет мир, а природа и прогресс существуют вместе.
Это не музей нулевых и не ироническая ностальгия.

Рабочая формула визуального языка:

- 55% Frutiger Aero — небо, вода, зелень, прозрачность и надежда;
- 25% Gen X Soft Club — воздух, редакционная сетка и транспортная навигация;
- 15% Y2K — полупрозрачный пластик, тактильные кнопки и аппаратные детали;
- 5% Metalheart — тёмный технический слой для ядра, ошибок и особых состояний.

## Принципы

1. Интерфейс ощущается как экспедиционный терминал, а не корпоративная админка.
2. Содержание остаётся читаемым без декоративного слоя.
3. Свет, стекло и изображение задают атмосферу; хром и сетки работают как акценты.
4. Metalheart не используется как постоянный фон: контраст должен оставаться редким.
5. Мобильная версия напоминает хорошую портативную консоль, но сохраняет крупные
   зоны нажатия и привычную навигацию.
6. Анимация не несёт обязательной информации и отключается через
   `prefers-reduced-motion`.

## Токены Hainan Aqua

| Роль | Значение |
| --- | --- |
| Sky | `#087ee8` |
| Deep sky | `#0754ae` |
| Aqua | `#0ccbd0` |
| Leaf | `#53c96b` |
| Lime signal | `#9ce94e` |
| Pearl | `#f8fdff` |
| Primary ink | `#062e50` |
| Technical dark | `#031d36` |

В админке основной шрифт интерфейса — системный `Segoe UI Variable`, заголовки,
коды и навигационные метки используют `Bahnschrift` с безопасными системными
fallback. Личный контур возвращает характер предыдущей версии приложения:

- интерфейс — `Trebuchet MS` → `Segoe UI`;
- системные коды — `Courier New` → `Lucida Console`;
- большие маршрутные заголовки — `Georgia`.

## Иконки личного контура

Нижняя навигация не использует современные line-icons. Для неё создан отдельный
набор оригинальных растровых объектов в духе интерфейсов 2004–2008 годов:
глянцевый полупрозрачный пластик, хромированный кант, внутренние блики и мягкая
аппаратная тень. Иконки сохраняются как PNG с прозрачным фоном и не содержат
текста, логотипов или чужих товарных знаков.

## Текущий styleframe

- вход — терминал прибытия Hainan с маршрутной и координатной навигацией;
- обзор — светлая экспедиционная приборная панель;
- карточка сезона — живая сцена Хайнаня, а не абстрактный градиент;
- технические панели плеера, сканера и закрытой витрины — редкие Metalheart-акценты;
- сгенерированная сцена не содержит текста, логотипов и интерфейсных элементов.

## Личный контур участника

Обновление 2026-09-08: для кейсов и коллекции сохранён текущий Aero/Y2K,
добавлены оконные панели, сегментная полоса, диалог выигрыша и папка предметов
в духе нулевых. Основной текст не пикселизуется. Дневная/ночная темы и
reduced-motion обязательны. Эти два экрана уже подключаются к игровым API
после входа; публичный режим остаётся только предпросмотром оформления.
Измеренная проверка и запуск — `V4_CASES.md`.

Первый статический styleframe стал основой `/app/`: Сегодня, REP, Магазин,
Кейсы, Задания и Ещё. На desktop
она выглядит как отдельное портативное устройство, на телефоне занимает весь
экран. Кейсы и коллекция уже используют личные API после входа; неподключённые
разделы не показывают фиктивных участников, события или баланс.

## Aero Desktop и движение — 2026-09-08

Ветка `codex/v4-aero-motion`, основа рабочих кейсов сохранена коммитом `b890dd1`.
Финальный слой — `zhidao_v4/static/app/aero-desktop.css`, контроллер движения —
`aero-motion.js`. Это расширение существующей ванильной оболочки, не замена
стека. Консоль Архитектора и игровой проект не менялись.

- Общая адресная строка `zhidao://…`, кнопки темы/движения и строка состояния.
  Это декоративная навигационная подпись, не настоящий адрес или индикатор
  исправности сервера. Нет сети определяется браузером.
- REP получил зелёный акцент, магазин — голубой, задания — фиолетовый.
  Оконные вкладки, перламутровые панели и хром объединяют остальные экраны.
  Витрина, задания и рейтинг всё ещё явно обозначены как неподключённые.
- На главной появились быстрые переходы и реальные попытки выбранного сезона;
  в профиле — реальные ★ и ссылка на коллекцию. Старый выдуманный прогноз
  температуры заменён декоративным островным знаком, не прогнозом погоды.
- Растровые значки и арты сохранены. Новые детали сделаны CSS, без чужих
  логотипов, скачанных элементов, масок, CDN и новых зависимостей. Геометрия
  карты и экономика не менялись.
- Движение: появление экранов/панелей, отклики кнопок, орбиты и луч сканера,
  раскрытие результата с оформлением его действительной редкости. Нет рулетки
  с фальшивыми призами, почти-выигрышами или изменения вероятностей клиентом.
- При быстром ответе визуальная последовательность занимает примерно 1,4 с
  до обновления данных/диалога: 750 мс до раскрытия + 650 мс раскрытия. Это
  заданные интервалы, не замер производительности. При медленном ответе фаза
  ожидания продолжается. Повторное получение результата не ждёт этих интервалов.
- Настройка `zhidao.v4.motion` хранится локально. Системный reduced-motion имеет
  приоритет; отключение убирает также старые бесконечные анимации. В скрытом
  документе CSS-анимации приостанавливаются, декоративные ожидания прерываются.
- Ключ открытия удаляется только после показа сохранённого результата в
  видимом документе. Перезагрузка во время раскрытия предлагает восстановление
  того же открытия; исключения декоративного раскрытия не отменяют награду.

### Воспроизводимая проверка

Локальный временный сервер: `.venv-web/Scripts/python.exe -m tools.preview_v4_cases
--port 8784`. База одноразовая; логины `case.student` / `case.operator`, пароль
`preview-only-passphrase`. Не запускать браузерные проверки против публикации
или реальной базы. Тесты выдают и расходуют попытки только тестового участника.

С установленным Playwright из bundled Node runtime:

```powershell
node tools/check_v4_cases_ui.cjs
node tools/check_v4_aero_ui.cjs
```

Запускать последовательно: оба используют одного тестового участника.
Харнессы самостоятельно восстанавливают запас попыток через интерфейс выдачи.

Измерено на Edge headless, Windows:

- 10 экранов × 2 темы × 5 ширин (320/360/390/768/1280): 100 проверок без
  горизонтального переполнения. Это проверка ширины страницы, не оценка всех
  возможных размеров и не художественное согласование.
- Обе браузерные проверки прошли: 6 проверок рабочих кейсов и 7 проверок
  оформления/движения; 0 JS exceptions и 0 зафиксированных CSP violations.
- Выключение движения переживает перезагрузку; при выключении и системном
  reduced-motion — 0 работающих анимаций. Выключение посреди открытия не
  оставляет кнопку заблокированной.
- Потерянный ответ после commit и перезагрузка во время раскрытия возвращают
  сохранённое открытие без дополнительного списания. В анимированном тесте
  отправлен 1 POST, название награды совпало с записью серверного журнала.
- Управляемый сценарий с задержкой ответа 1200 мс занял 3597 мс, включая
  скриншот во время сканирования. Это не FPS и не скорость на телефоне.
- Python: 56 passed, 1678 subtests passed (cases/auth/MAX auth/assets/map),
  1 прежнее предупреждение Starlette/httpx; синтаксис JS и asset stamps прошли.

Скриншоты, JSON-отчёты и ролик `aero-tour.webm` находятся в
`.codex-tmp/aero-qa/`; регрессия кейсов — `.codex-tmp/cases-qa/` (не в git).
Реальный WebView MAX и скорость на устройстве не проверены. Публикация не
обновлялась; визуальное одобрение и проверка в MAX остаются перед выкладкой.
