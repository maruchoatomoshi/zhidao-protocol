# ZHIDAO V4 — «Обещанное завтра»

## Идея

ZHIDAO Hainan показывает будущее, которое всё-таки наступило: технологии
дружелюбны, путешествие расширяет мир, а природа и прогресс существуют вместе.
Это не музей нулевых и не ироническая ностальгия.

Рабочая формула визуального языка:

- 55% Frutiger Aero — небо, вода, зелень, прозрачность и надежда;
- 25% Gen X Soft Club — воздух, редакционная сетка и транспортная навигация;
- 15% Y2K — полупрозрачный пластик, тактильные кнопки и аппаратные детали;
- 5% Metalheart — тёмный технический слой для ядра, ошибок и особых состояний.

## Принципы

1. Интерфейс ощущается как экспедиционный терминал, а не корпоративная админка.
2. Содержание остаётся читаемым без декоративного слоя.
3. Свет, стекло и изображение задают атмосферу; хром и сетки работают как акценты.
4. Metalheart не используется как постоянный фон: контраст должен оставаться редким.
5. Мобильная версия напоминает хорошую портативную консоль, но сохраняет крупные
   зоны нажатия и привычную навигацию.
6. Анимация не несёт обязательной информации и отключается через
   `prefers-reduced-motion`.

## Токены Hainan Aqua

| Роль | Значение |
| --- | --- |
| Sky | `#087ee8` |
| Deep sky | `#0754ae` |
| Aqua | `#0ccbd0` |
| Leaf | `#53c96b` |
| Lime signal | `#9ce94e` |
| Pearl | `#f8fdff` |
| Primary ink | `#062e50` |
| Technical dark | `#031d36` |

В админке основной шрифт интерфейса — системный `Segoe UI Variable`, заголовки,
коды и навигационные метки используют `Bahnschrift` с безопасными системными
fallback. Личный контур возвращает характер предыдущей версии приложения:

- интерфейс — `Trebuchet MS` → `Segoe UI`;
- системные коды — `Courier New` → `Lucida Console`;
- большие маршрутные заголовки — `Georgia`.

## Иконки личного контура

Нижняя навигация не использует современные line-icons. Для неё создан отдельный
набор оригинальных растровых объектов в духе интерфейсов 2004–2008 годов:
глянцевый полупрозрачный пластик, хромированный кант, внутренние блики и мягкая
аппаратная тень. Иконки сохраняются как PNG с прозрачным фоном и не содержат
текста, логотипов или чужих товарных знаков.

## Текущий styleframe

- вход — терминал прибытия Hainan с маршрутной и координатной навигацией;
- обзор — светлая экспедиционная приборная панель;
- карточка сезона — живая сцена Хайнаня, а не абстрактный градиент;
- системное ядро — единственная постоянная зона с тёмным Metalheart-акцентом;
- сгенерированная сцена не содержит текста, логотипов и интерфейсных элементов.

## Личный контур участника

Первый статический styleframe доступен по `/app/`. Это кликабельная оболочка из
шести будущих разделов: Сегодня, REP, Магазин, Кейсы, Задания и Ещё. На desktop
она выглядит как отдельное портативное устройство, на телефоне занимает весь
экран. Пока оболочка намеренно не запрашивает API и не показывает фиктивных
участников, события или баланс.
